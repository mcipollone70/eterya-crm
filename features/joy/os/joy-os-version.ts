/** Semantic version of the OS contract (not npm). */
export const JOY_OS_VERSION = "2.1.0-command-center";

export const JOY_OS_PRINCIPLES = [
  "Never ask «Cosa vuoi fare?» — always advise.",
  "Learn only from real CRM aggregates.",
  "Never invent facts or forecasts as promises.",
  "Never auto-save mutations without confirmation.",
  "Re-evaluate on day start/end, visit end, free slots.",
  "Every suggestion exposes action, reason, dataUsed, urgency, confidence, missingData.",
  "Joy can contradict («Non te lo consiglio») with data; agent may override.",
  "CRM is the invisible data engine — Joy is the operational interface.",
] as const;
